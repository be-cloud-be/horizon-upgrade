# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openupgrade: https://github.com/OCA/OpenUpgrade.git
    odoo: https://github.com/odoo/odoo.git
    becloud : https://github.com/be-cloud-be/odoo.git
  target: odoo $ODOO_VERSION
  merges:
    - odoo $ODOO_VERSION
    # - odoo refs/pull/80570/head
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
  shell_command_after:
    - git fetch becloud 57be31179d83a715835bc242f9401396d62e3563
    - git cherry-pick 57be31179d83a715835bc242f9401396d62e3563
    - git fetch becloud 92321c993bf59d5428118b8cf9fd6fd8913747ef
    - git cherry-pick 92321c993bf59d5428118b8cf9fd6fd8913747ef
    - git fetch becloud 4a28a8e5e055ec0330f62400198e3b9b63218cf6
    - git cherry-pick 4a28a8e5e055ec0330f62400198e3b9b63218cf6
    
web:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/web.git
    tecnativa: https://github.com/Tecnativa/partner-contact.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    
partner-contact:
  defaults:
    # Shallow repositories (1=1) are faster & thinner
    # You may need a bigger depth when merging PRs
    depth: "$DEPTH_DEFAULT"
  remotes:
    partner-contact: https://github.com/OCA/partner-contact.git
  target:
    partner-contact $ODOO_VERSION
  merges:
    - partner-contact $ODOO_VERSION

horizon:
  defaults:
    # Shallow repositories (1=1) are faster & thinner
    # You may need a bigger depth when merging PRs
    depth: "$DEPTH_DEFAULT"
  remotes:
    horizon: https://github.com/be-cloud-be/horizon.git
  target:
    horizon $ODOO_VERSION
  merges:
    - horizon $ODOO_VERSION
    
server-tools:
  defaults:
    # Shallow repositories (1=1) are faster & thinner
    # You may need a bigger depth when merging PRs
    depth: "$DEPTH_DEFAULT"
  remotes:
    server-tools: https://github.com/OCA/server-tools.git
  target:
    server-tools $ODOO_VERSION
  merges:
    - server-tools $ODOO_VERSION